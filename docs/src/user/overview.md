# Overview

Users interact with Orbit by issuing commands through the command-line interface. 

The following is an example workflow that identifies which Orbit commands could be used and when they should be used during an ip's development cycle:

![](./../images/command-flow.svg)
